# Supabase Project URL (Project Settings -> API)
VITE_SUPABASE_URL=

# Public key only. In newer Supabase dashboards this is shown as the
# "publishable key" and starts with sb_publishable_. Never use service_role.
VITE_SUPABASE_ANON_KEY=
